{
	"name": "wp-plugin-directory-access",
	"version": "1.0.0",
	"description": "A WordPress plugin that allows you to manage your files in real directories.",
	"license": "MIT",
	"scripts": {
		"build": "wp-scripts build -o dist",
		"watch": "npm run build -- --mode development --watch"
	},
	"devDependencies": {
		"@types/jquery": "^3.5.30",
		"@types/jqueryui": "^1.12.23",
		"@wordpress/scripts": "^30.7.0"
	}
}
